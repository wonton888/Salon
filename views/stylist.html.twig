<!DOCTYPE html>
<html>
    <head>
        <title>Clients</title>
    </head>
    <body>
        <h1>{{ stylist.getName() }}</h1>
        {% for client in clients %}
        <li>Client listed: <a href="/client/{{ client.getId }}/edit">{{ client.getName }}</a></li>
        {% endfor %}

        <h3>Add a client to your hair stylist.</h3>

        <form action="/stylists/{{ stylist.getId() }}/add_client" method="post">
            <input id="stylist_id" name="stylist_id"  type="hidden" value="{{ stylist.getId() }}">

            <label for="client_name">Add Client's Name</label>
            <input id="client_name" name="client_name" type="text">
            <button type="submit" name="button">Add Client's name</button>
        </form>

        <form action="/delete_clients" method="post">
          <button type="submit" name="button">Clear</button>
        </form>

        <p>
            <a href="/">Home</a>
            <a href="/stylists/{{ stylist.getId }}/edit">Edit this Stylist!</a>
        </p>
    </body>
</html>
